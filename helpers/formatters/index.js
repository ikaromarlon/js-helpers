const getFloat = require('./getFloat');
const getNumbers = require('./getNumbers');
const numberFormat = require('./numberFormat');
const removeAccents = require('./removeAccents');
const replaceLast = require('./replaceLast');
const stripTags = require('./stripTags');
const uppercaseFirst = require('./uppercaseFirst');

module.exports = {
  getFloat,
  getNumbers,
  numberFormat,
  removeAccents,
  replaceLast,
  stripTags,
  uppercaseFirst,
};
